# Force browsers to always revalidate HTML/JS/CSS with the server instead of
# serving a stale cached copy after deploys.
<IfModule mod_headers.c>
  <FilesMatch "\.(html|htm|jsx|css)$">
    Header set Cache-Control "no-cache, must-revalidate"
  </FilesMatch>
</IfModule>
